[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Client-side routing: serve index.html for any path so /faq, /rsvp,
# and shared links work on a direct visit or refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
