# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Original scrapbook photos. Only the optimised copies that `npm run art:photos`
# writes into public/images/scrapbook/ need to be committed and deployed, so the
# full-resolution originals stay out of the repo.
photos/

# Screenshots taken while checking a page
shot-*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
